<%- if @user.albums.find_by_id(@album.id) %>
    $("input.favourite_toggle[id=<%= @album.id.to_s %>]").attr("value", "Unfavourite");
<%- else %>
    $("input.favourite_toggle[id=<%= @album.id.to_s %>]").attr("value", "Favourite");
<%- end  %>